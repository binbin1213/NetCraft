# 测试清单

## 1. 测试范围

| 验收标准编号 | 覆盖的验收标准 |
|--------------|----------------|
| 1.1          | JWT 密钥从环境变量读取，代码中无硬编码敏感信息 |
| 1.2          | 登录/鉴权流程正常工作 |
| 2.1          | 开发环境下 CORS 允许本地访问 |
| 2.2          | 生产环境下 CORS 仅允许配置的域名访问 |
| 3.1          | 现有功能不受修改影响 |

## 2. 用例清单

### 2.1 登录/鉴权流程测试

| 用例编号 | 测试场景 | 测试步骤 | 预期结果 | 风险等级 |
|----------|----------|----------|----------|----------|
| 1.1.1    | 正常登录流程 | 1. 设置 SECRET_KEY 环境变量<br>2. 启动后端服务<br>3. 发送注册请求<br>4. 使用注册的用户名密码登录<br>5. 使用返回的 JWT 访问受保护资源 | 1. 服务正常启动<br>2. 注册成功<br>3. 登录成功，返回 JWT<br>4. 成功访问受保护资源 | P0 |
| 1.1.2    | 未设置 SECRET_KEY 时启动服务 | 1. 确保未设置 SECRET_KEY 环境变量<br>2. 尝试启动后端服务 | 服务启动失败，报错 "SECRET_KEY environment variable is not set" | P0 |
| 1.1.3    | 无效 JWT 访问受保护资源 | 1. 正常启动服务并登录<br>2. 手动修改 JWT 令牌<br>3. 使用修改后的 JWT 访问受保护资源 | 返回 401 Unauthorized | P0 |
| 1.1.4    | 过期 JWT 访问受保护资源 | 1. 配置短过期时间的 SECRET_KEY<br>2. 登录获取 JWT<br>3. 等待 JWT 过期<br>4. 使用过期 JWT 访问受保护资源 | 返回 401 Unauthorized | P1 |
| 1.1.5    | 空 JWT 访问受保护资源 | 1. 不提供 JWT 令牌<br>2. 访问受保护资源 | 返回 401 Unauthorized | P1 |

### 2.2 CORS 行为测试

| 用例编号 | 测试场景 | 测试步骤 | 预期结果 | 风险等级 |
|----------|----------|----------|----------|----------|
| 2.1.1    | 开发环境下本地访问 | 1. 启动服务，使用默认 ALLOWED_ORIGINS<br>2. 从 http://localhost:5173 发送请求<br>3. 检查响应头中的 Access-Control-Allow-Origin | 响应头包含 Access-Control-Allow-Origin: http://localhost:5173 | P1 |
| 2.1.2    | 生产环境下配置域名访问 | 1. 设置 ALLOWED_ORIGINS=https://example.com<br>2. 启动服务<br>3. 从 https://example.com 发送请求<br>4. 检查响应头 | 响应头包含 Access-Control-Allow-Origin: https://example.com | P0 |
| 2.1.3    | 未配置域名访问生产环境 | 1. 设置 ALLOWED_ORIGINS=https://example.com<br>2. 启动服务<br>3. 从 http://localhost:5173 发送请求<br>4. 检查浏览器控制台 | 浏览器控制台显示 CORS 错误，请求被阻止 | P1 |
| 2.1.4    | 多域名配置测试 | 1. 设置 ALLOWED_ORIGINS=https://example1.com,https://example2.com<br>2. 启动服务<br>3. 分别从两个域名发送请求<br>4. 检查响应头 | 两个域名的请求都成功，响应头包含对应的 Origin | P1 |
| 2.1.5    | 通配符配置测试 | 1. 设置 ALLOWED_ORIGINS=*<br>2. 启动服务<br>3. 从任意域名发送请求<br>4. 检查响应头 | 响应头包含 Access-Control-Allow-Origin: * | P1 |

### 2.3 回归测试

| 用例编号 | 测试场景 | 测试步骤 | 预期结果 | 风险等级 |
|----------|----------|----------|----------|----------|
| 3.1.1    | 项目CRUD操作 | 1. 正常登录<br>2. 创建项目<br>3. 获取项目列表<br>4. 更新项目<br>5. 删除项目 | 所有操作成功，返回正确的响应 | P0 |
| 3.1.2    | AI聊天功能 | 1. 正常登录<br>2. 发送AI聊天请求<br>3. 检查AI响应 | AI聊天功能正常工作 | P1 |
| 3.1.3    | 用户注册功能 | 1. 发送注册请求<br>2. 检查注册结果<br>3. 尝试使用相同用户名再次注册 | 1. 注册成功<br>2. 重复注册失败，返回适当错误信息 | P1 |
| 3.1.4    | 密码哈希验证 | 1. 注册用户<br>2. 直接检查数据库中密码存储<br>3. 使用正确密码登录<br>4. 使用错误密码登录 | 1. 密码以哈希形式存储，非明文<br>2. 正确密码登录成功<br>3. 错误密码登录失败 | P0 |

## 3. 回归清单

| 回归点 | 影响范围 | 测试方法 | 风险等级 |
|--------|----------|----------|----------|
| 用户认证系统 | 所有受保护资源 | 验证登录、注册、JWT验证功能正常 | P0 |
| 项目管理功能 | 项目CRUD操作 | 验证项目创建、读取、更新、删除功能正常 | P0 |
| AI聊天功能 | AI服务调用 | 验证AI聊天功能正常 | P1 |
| 数据库连接 | 所有数据操作 | 验证服务能正常连接数据库，执行CRUD操作 | P0 |
| 服务启动流程 | 整个后端服务 | 验证服务能正常启动，无启动错误 | P0 |

## 4. 测试执行指南

### 4.1 环境准备

1. 确保已安装 Docker 和 Docker Compose
2. 克隆仓库到本地：`git clone https://github.com/binbin1213/netcraft.git && cd netcraft`
3. 准备测试环境变量文件 `.env.test`

### 4.2 运行测试

#### 4.2.1 单元测试

```bash
# 进入backend目录
cd backend

# 安装依赖
pip install -r requirements.txt

# 运行测试（如果有）
pytest
```

#### 4.2.2 集成测试

```bash
# 设置测试环境变量
export SECRET_KEY=test_secret_key
export ALLOWED_ORIGINS=http://localhost:5173

# 启动服务
docker-compose up --build -d

# 运行API测试（使用curl或Postman）
# 示例：注册用户
curl -X POST http://localhost/api/users/ -H "Content-Type: application/json" -d '{"username":"testuser","password":"Test123!"}'

# 示例：登录
curl -X POST http://localhost/api/token/ -H "Content-Type: application/x-www-form-urlencoded" -d "username=testuser&password=Test123!"

# 停止服务
docker-compose down
```

#### 4.2.3 CORS测试

```bash
# 测试开发环境CORS
export ALLOWED_ORIGINS=http://localhost:5173
docker-compose up --build -d
# 使用浏览器从http://localhost:5173访问API，检查控制台是否有CORS错误

# 测试生产环境CORS
export ALLOWED_ORIGINS=https://example.com
docker-compose up --build -d
# 使用curl模拟不同域名请求
curl -X GET http://localhost/api/ -H "Origin: http://localhost:5173" -v
curl -X GET http://localhost/api/ -H "Origin: https://example.com" -v
```

## 5. 可观测性要求

### 5.1 日志要求

- 服务启动时应记录 SECRET_KEY 是否已设置
- 服务启动时应记录 CORS 配置信息
- 认证失败时应记录详细错误信息
- 关键操作（如用户注册、登录）应记录日志

### 5.2 监控要求

- 监控 JWT 认证成功率
- 监控 CORS 错误率
- 监控服务启动成功率

## 6. 测试工具推荐

1. **API测试工具**：Postman、curl、Httpie
2. **自动化测试框架**：pytest（后端）、Jest（前端）
3. **CORS测试工具**：浏览器控制台、curl、CORS Test工具
4. **环境管理工具**：dotenv、Docker Compose
